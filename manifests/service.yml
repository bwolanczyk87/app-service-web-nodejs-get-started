apiVersion: v1
kind: Service
metadata:
    name: bwolanczykappservicewebnodejsgetstarted
spec:
    type: LoadBalancer
    ports:
    - port: 3000 
    selector:
        app: bwolanczykappservicewebnodejsgetstarted